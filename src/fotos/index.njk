---
layout: base
title: "Pauls Fotoalbum voller alltäglichen Scenen und Geschichten | Paul Borm"
pagination:
  data: photos
  size: 7
bodyClass: ["page--photo"]
changeFreq: "weekly"
---

{% from 'photos-article.njk' import article %}

<section class="page-main-intro">
  <h1>
    Alltägliche Scenen und Geschichten fotografiert mit meiner Kamera, dann hier im Album abgelegt.
  </h1>
</section>

<section class="page-main-content">
  {% for datetime, photos in pagination.items %}
    {{ article(datetime, photos) }}
  {% endfor %}

  {% if pagination.pages | length > 1 %}
    {% include "pagination.njk" %}
  {% endif %}
</section>
