{% macro framedImage (src, alt='', caption='', id='', class='', dataID='') %}
<figure data-component="pauls-framed-image" {% if dataID %}data-id="{{ dataID }}"{% endif %} class="frame {{ class }}">
  <img
    id="{{ id }}"
    src="{{ ['/', src] | join() | url }}"
    alt="{{ alt }}"
    loading="lazy"
    decoding="async"
  />
  {%- if caption %}
  <figcaption>
    {{ caption | safe}}
  </figcaption>
  {% endif -%}
</figure>
{% endmacro %}
